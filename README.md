# cobol-bf
Brainfuck interpreter written in Cobol.

## Expected functionality

 - Fully functional interpreter capable of running `Hanoi.bf` and `mandelbrot.bf`
 - Simple CLI with
   * Run code from stdin in non-interactive mode
   * Run code from file in interactive mode
   * Output to file
 - Minimal error handling (missing brackets, negative memory access and etc.)
